export interface IForm {
    class?: string;
    type?: string;
    controlname?: string;
    data?: any[];
    isMultiType?: boolean
    search?: boolean
    header?: string;
    min?: number,
    max?: number,
    rows?: string,
    ScrollHeightStatus?: boolean,
    ScrollHeight?: string
}