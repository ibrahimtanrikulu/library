export enum FormTypeEnum {
  text = 'text',
  number = 'number',
  textarea = 'textarea',
  password = 'password',
  checkbox = 'checkbox',
  dropdown = 'dropdown',
  checkboxList = 'checkboxList',
  button = 'button',
  switch = 'switch',
}
